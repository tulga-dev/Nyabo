"""Evals: golden set, runners, metrics, the corrections nightly job (docs/ARCHITECTURE.md §8).

``nyabo_mn.evals.run.run`` is the entry point; ``corrections_job.run_nightly`` is the
scheduler hook. Nothing here is imported by the request path.
"""
